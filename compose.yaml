services:
  caddy:
    image: caddy:2.8.4-alpine
    container_name: caddy
    networks:
      - homelab
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy:/data/
      - ./config/caddy/Caddyfile:/etc/caddy/Caddyfile
      - ./config/caddy/extras:/etc/caddy/extras
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    command: --config /data/settings.json --address 0.0.0.0
    networks:
      - homelab
    restart: unless-stopped
    volumes:
      - ./data:/data/files
      - ./config/filebrowser/settings.json:/data/settings.json
      - ./config/filebrowser/database.db:/data/database.db
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
  silverbullet:
    image: ghcr.io/silverbulletmd/silverbullet
    container_name: silverbullet
    networks:
      - homelab
    restart: unless-stopped
    environment:
      SB_URL_PREFIX: "/silverbullet"
      PUID: "${PUID}"
      PGID: "${PGID}"
    volumes:
      - ./data/notes:/space

volumes:
  caddy:

networks:
  homelab:
    name: homelab
    driver: bridge